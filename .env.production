# ============================================================
# kk_nl2sql_aibot 生产环境变量
# 用法: cp .env.production .env && 编辑实际值
# 启动: docker compose -f docker-compose.prod.yml --env-file .env up -d
# ============================================================

# --- PostgreSQL ---
POSTGRES_USER=kk_nl2sql
POSTGRES_PASSWORD=<CHANGE-ME-STRONG-PASSWORD>
POSTGRES_DB=kk_nl2sql

# --- MinIO (业务文件存储) ---
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=<CHANGE-ME-STRONG-PASSWORD>

# --- MemOS Cloud (记忆系统) ---
MEMOS_API_URL=https://api.openmem.net
MEMOS_API_KEY=<YOUR-MEMOS-API-KEY>

# --- LLM API Keys ---
DEEPSEEK_API_KEY=<YOUR-DEEPSEEK-KEY>
QWEN_API_KEY=<YOUR-QWEN-KEY>

# --- JWT ---
JWT_SECRET_KEY=<CHANGE-ME-GENERATE-WITH-openssl-rand-hex-32>

# --- CORS (生产域名) ---
CORS_ORIGINS=["https://yourdomain.com"]

# --- Gunicorn Workers ---
WORKERS=2

# --- 端口映射 (生产环境只暴露 80/443) ---
HTTP_PORT=80
